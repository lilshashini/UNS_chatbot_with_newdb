-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.dashboard_status (
  id bigint NOT NULL DEFAULT nextval('dashboard_status_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  oee numeric,
  availability numeric,
  performance numeric,
  quality numeric,
  current_batch_status character varying,
  maintenance_status character varying,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT dashboard_status_pkey PRIMARY KEY (id),
  CONSTRAINT dashboard_status_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.dashboard_status_history (
  history_id bigint NOT NULL DEFAULT nextval('dashboard_status_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  oee numeric,
  availability numeric,
  performance numeric,
  quality numeric,
  current_batch_status character varying,
  maintenance_status character varying,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT dashboard_status_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.departments (
  id integer NOT NULL DEFAULT nextval('departments_id_seq'::regclass),
  site_id integer,
  department_name character varying NOT NULL,
  shifts integer,
  shift_hours jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT departments_pkey PRIMARY KEY (id),
  CONSTRAINT departments_site_id_fkey FOREIGN KEY (site_id) REFERENCES public.sites(id)
);
CREATE TABLE public.departments_history (
  history_id bigint NOT NULL DEFAULT nextval('departments_history_history_id_seq'::regclass),
  orig_id bigint,
  site_id integer,
  department_name character varying,
  shifts integer,
  shift_hours jsonb,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT departments_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.erp_orders (
  id bigint NOT NULL DEFAULT nextval('erp_orders_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  order_number integer,
  order_status character varying,
  scheduled_start_time timestamp without time zone,
  scheduled_end_time timestamp without time zone,
  actual_start_time timestamp without time zone,
  actual_end_time timestamp without time zone,
  produced_quantity integer,
  remaining_quantity integer,
  item_number integer,
  item_description text,
  bom character varying,
  available_quantity integer,
  reserved_quantity integer,
  ordered_quantity integer,
  location character varying,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT erp_orders_pkey PRIMARY KEY (id),
  CONSTRAINT erp_orders_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.erp_orders_history (
  history_id bigint NOT NULL DEFAULT nextval('erp_orders_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  order_number integer,
  order_status character varying,
  scheduled_start_time timestamp without time zone,
  scheduled_end_time timestamp without time zone,
  actual_start_time timestamp without time zone,
  actual_end_time timestamp without time zone,
  produced_quantity integer,
  remaining_quantity integer,
  item_number integer,
  item_description text,
  bom character varying,
  available_quantity integer,
  reserved_quantity integer,
  ordered_quantity integer,
  location character varying,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT erp_orders_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.iso55001_metrics (
  id bigint NOT NULL DEFAULT nextval('iso55001_metrics_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  status character varying,
  maintenance_schedule date,
  risk_level character varying,
  mitigation_plan character varying,
  oee numeric,
  mtbf integer,
  mttr integer,
  regulatory_status character varying,
  last_review_date date,
  planned_action character varying,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT iso55001_metrics_pkey PRIMARY KEY (id),
  CONSTRAINT iso55001_metrics_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.iso55001_metrics_history (
  history_id bigint NOT NULL DEFAULT nextval('iso55001_metrics_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  status character varying,
  maintenance_schedule date,
  risk_level character varying,
  mitigation_plan character varying,
  oee numeric,
  mtbf integer,
  mttr integer,
  regulatory_status character varying,
  last_review_date date,
  planned_action character varying,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT iso55001_metrics_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.kpi_metrics (
  id bigint NOT NULL DEFAULT nextval('kpi_metrics_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  source character varying NOT NULL,
  oee numeric,
  availability numeric,
  performance numeric,
  quality numeric,
  teep numeric,
  mtbf integer,
  mttr integer,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT kpi_metrics_pkey PRIMARY KEY (id),
  CONSTRAINT kpi_metrics_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.kpi_metrics_history (
  history_id bigint NOT NULL DEFAULT nextval('kpi_metrics_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  source character varying,
  oee numeric,
  availability numeric,
  performance numeric,
  quality numeric,
  teep numeric,
  mtbf integer,
  mttr integer,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT kpi_metrics_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.maintenance_records (
  id bigint NOT NULL DEFAULT nextval('maintenance_records_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  source character varying NOT NULL,
  machine_id character varying,
  maintenance_status character varying,
  last_maintenance_date timestamp without time zone,
  next_maintenance_date timestamp without time zone,
  maintenance_history text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT maintenance_records_pkey PRIMARY KEY (id),
  CONSTRAINT maintenance_records_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.maintenance_records_history (
  history_id bigint NOT NULL DEFAULT nextval('maintenance_records_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  source character varying,
  machine_id character varying,
  maintenance_status character varying,
  last_maintenance_date timestamp without time zone,
  next_maintenance_date timestamp without time zone,
  maintenance_history text,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT maintenance_records_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.process_variables (
  id bigint NOT NULL DEFAULT nextval('process_variables_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  source character varying NOT NULL,
  spindle_speed integer,
  feed_rate numeric,
  tool_wear numeric,
  coolant_temperature numeric,
  vibration numeric,
  power_consumption numeric,
  tool_change_count integer,
  material_temperature numeric,
  part_dimensions character varying,
  surface_finish character varying,
  infeed numeric,
  outfeed numeric,
  waste numeric,
  state integer,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT process_variables_pkey PRIMARY KEY (id),
  CONSTRAINT process_variables_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.process_variables_history (
  history_id bigint NOT NULL DEFAULT nextval('process_variables_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  source character varying,
  spindle_speed integer,
  feed_rate numeric,
  tool_wear numeric,
  coolant_temperature numeric,
  vibration numeric,
  power_consumption numeric,
  tool_change_count integer,
  material_temperature numeric,
  part_dimensions character varying,
  surface_finish character varying,
  infeed numeric,
  outfeed numeric,
  waste numeric,
  state integer,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT process_variables_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.production_lines (
  id integer NOT NULL DEFAULT nextval('production_lines_id_seq'::regclass),
  department_id integer,
  line_name character varying NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT production_lines_pkey PRIMARY KEY (id),
  CONSTRAINT production_lines_department_id_fkey FOREIGN KEY (department_id) REFERENCES public.departments(id)
);
CREATE TABLE public.production_lines_history (
  history_id bigint NOT NULL DEFAULT nextval('production_lines_history_history_id_seq'::regclass),
  orig_id bigint,
  department_id integer,
  line_name character varying,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT production_lines_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.quality_inspections (
  id bigint NOT NULL DEFAULT nextval('quality_inspections_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  source character varying NOT NULL,
  order_number integer,
  item_number integer,
  inspection_result character varying,
  rejection_reason character varying,
  rejection_quantity integer,
  accepted_quantity integer,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT quality_inspections_pkey PRIMARY KEY (id),
  CONSTRAINT quality_inspections_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.quality_inspections_history (
  history_id bigint NOT NULL DEFAULT nextval('quality_inspections_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  source character varying,
  order_number integer,
  item_number integer,
  inspection_result character varying,
  rejection_reason character varying,
  rejection_quantity integer,
  accepted_quantity integer,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT quality_inspections_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.s88_batch_control (
  id bigint NOT NULL DEFAULT nextval('s88_batch_control_id_seq'::regclass),
  line_id integer,
  timestamp timestamp without time zone NOT NULL,
  source character varying NOT NULL,
  batch_mixing_tank_status character varying,
  bottler_status character varying,
  capper_status character varying,
  temperature_controller character varying,
  volume_control character varying,
  soda_recipe character varying,
  production_parameters character varying,
  operator_interface_status character varying,
  process_data character varying,
  quality_data character varying,
  safety_status character varying,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT s88_batch_control_pkey PRIMARY KEY (id),
  CONSTRAINT s88_batch_control_line_id_fkey FOREIGN KEY (line_id) REFERENCES public.production_lines(id)
);
CREATE TABLE public.s88_batch_control_history (
  history_id bigint NOT NULL DEFAULT nextval('s88_batch_control_history_history_id_seq'::regclass),
  orig_id bigint,
  line_id integer,
  timestamp timestamp without time zone,
  source character varying,
  batch_mixing_tank_status character varying,
  bottler_status character varying,
  capper_status character varying,
  temperature_controller character varying,
  volume_control character varying,
  soda_recipe character varying,
  production_parameters character varying,
  operator_interface_status character varying,
  process_data character varying,
  quality_data character varying,
  safety_status character varying,
  created_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT s88_batch_control_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.sites (
  id integer NOT NULL DEFAULT nextval('sites_id_seq'::regclass),
  site_name character varying NOT NULL UNIQUE,
  gm character varying,
  bu character varying,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT sites_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sites_history (
  history_id bigint NOT NULL DEFAULT nextval('sites_history_history_id_seq'::regclass),
  orig_id bigint,
  site_name character varying,
  gm character varying,
  bu character varying,
  created_at timestamp without time zone,
  updated_at timestamp without time zone,
  operation character varying,
  changed_at timestamp without time zone DEFAULT now(),
  changed_by text,
  CONSTRAINT sites_history_pkey PRIMARY KEY (history_id)
);
CREATE TABLE public.unified_json_history (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  uns json,
  CONSTRAINT unified_json_history_pkey PRIMARY KEY (id)
);
CREATE TABLE public.uns_lvl_factory_current (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  factory character varying UNIQUE,
  received_uns json,
  CONSTRAINT uns_lvl_factory_current_pkey PRIMARY KEY (id)
);
CREATE TABLE public.uns_lvl_factory_history (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  factory character varying,
  received_uns json,
  CONSTRAINT uns_lvl_factory_history_pkey PRIMARY KEY (id)
);